# Fbspider 像素授权系统 — 测试人员本地部署指南

---

## 📋 概述

本系统用于通过 Chrome 浏览器插件自动化完成 Facebook 像素的分享操作。测试人员通过 OpenClaw 输入自然语言指令，系统自动完成像素分享。

**完整链路**：

```
测试人员在 OpenClaw 输入自然语言
         ↓
    OpenClaw 解析意图，调用 Skill
         ↓
    Skill 调用后端 HTTP API（localhost:7150）
         ↓
    后端通过 WebSocket 转发指令到 Chrome 插件
         ↓
    插件调用 Facebook GraphQL 执行分享
         ↓
    结果逐层返回给 OpenClaw
```

**部署步骤（按顺序执行）**：

1. 安装 Docker Desktop
2. 部署后端服务
3. 安装 Chrome 插件
4. 配置 OpenClaw + Skill
5. 测试功能

---

## 💻 环境要求

| 项目 | 要求 |
| :--- | :--- |
| 操作系统 | Windows 10 / 11（64 位） |
| 浏览器 | Google Chrome（最新版） |
| 内存 | 至少 8GB |
| 磁盘 | 至少 5GB 可用空间 |
| 网络 | 能访问 Facebook 和 Docker Hub |
| Python | 3.9+（OpenClaw 已自带，无需额外安装） |

---

## 📦 准备物料清单

测试人员会收到以下文件（打包为压缩包或网盘下载）：

```
fbspider-test-kit/
├── fbspider v2.2.4/              ← Chrome 插件文件夹（整个目录）
├── fbspider-server/               ← 后端服务文件夹（整个目录）
│   └── fbspider_pixel_authorize.py  ← Skill 文件（也在这个目录内）
└── 测试人员本地部署指南.md        ← 本文档
```

测试人员电脑上已有的：

- ✅ OpenClaw（全新安装版本）

---

## 🔧 第一步：安装 Docker Desktop

### 1.1 下载

- 访问 Docker 官网下载页：https://www.docker.com/products/docker-desktop/
- 点击 **Download for Windows** 下载安装包

### 1.2 安装

1. 双击 `Docker Desktop Installer.exe`
2. 安装过程中保持默认选项，一路点 **Next**
3. 安装完成后，**重启电脑**

### 1.3 验证安装

1. 重启后，打开 **Docker Desktop** 应用（桌面有图标，或从开始菜单搜索）
2. 等待左下角状态显示 **Engine running**（绿色）
3. 打开命令提示符（按 `Win+R`，输入 `cmd`，回车），输入：

```
docker --version
```

应显示版本号，如 `Docker version 29.x.x`

> ⚠️ **常见问题**：如果 Docker Desktop 提示需要 WSL2 更新，按提示点击安装即可。

---

## 🚀 第二步：部署后端服务

### 2.1 启动后端

1. 打开 **命令提示符**（按 `Win+R`，输入 `cmd`，回车）
2. 切换到后端目录（假设文件放在 D 盘）：

```
cd /d D:\fbspider-test-kit\fbspider-server
```

3. 启动服务（首次启动需要下载镜像，约 2-5 分钟）：

```
docker compose up -d --build
```

等待输出显示 `Container fbhelper-server Started` 即成功。

### 2.2 验证后端

打开浏览器，访问：

```
http://localhost:7150
```

能看到页面（显示"本地面板"或类似内容）即说明后端启动成功。

### 2.3 日常操作

| 操作 | 命令（在 fbspider-server 目录下执行） |
| :--- | :--- |
| 启动服务 | `docker compose up -d` |
| 停止服务 | `docker compose down` |
| 重启服务 | `docker compose restart` |
| 查看状态 | `docker compose ps` |
| 查看日志 | `docker compose logs --tail=50` |

> 💡 **提示**：每次电脑重启后，如果 Docker Desktop 设置了开机自启，容器会自动运行。否则需要手动打开 Docker Desktop。

---

## 🧩 第三步：安装 Chrome 插件

### 3.1 打开扩展程序页面

1. 打开 Chrome 浏览器
2. 地址栏输入 `chrome://extensions/` 并回车

### 3.2 开启开发者模式

- 页面右上角有一个 **"开发者模式"** 开关，点击开启

### 3.3 加载插件

1. 点击左上角 **"加载已解压的扩展程序"** 按钮
2. 在弹出的文件选择窗口中，选择 `fbspider v2.2.4` 文件夹
3. 点击 **"选择文件夹"**

插件列表中会出现 **fbspider 2.2.4**，状态为"已启用"。

### 3.4 验证插件连接

1. 打开一个新标签页，访问 `https://fbspider.com`
2. 按 `F12` 打开开发者工具
3. 切到 **Console** 标签
4. 应能看到类似日志：

```
[PixelShare] 模块已加载, device_id=px_xxxxxxxx
[PixelShare] WS 已连接
[PixelShare] WS 注册设备: px_xxxxxxxx
```

看到 `WS 已连接` 即说明插件与后端通信正常 ✅

---

## 🤖 第四步：配置 OpenClaw + Skill

### 4.1 复制 Skill 文件

将 `fbspider-server` 目录下的 `fbspider_pixel_authorize.py` 文件复制到 OpenClaw 的 Skill 目录中：

1. 找到 OpenClaw 的安装目录（通常在 `C:\Users\你的用户名\.openclaw\skills\` 或类似路径）
2. 将 `fbspider_pixel_authorize.py` 复制到该目录

> 💡 如果不确定 Skill 目录在哪，打开 OpenClaw 后在设置中查看 "Skills 路径" 或询问开发人员。

### 4.2 配置环境变量

在 OpenClaw 的配置中（或系统环境变量中），设置以下两项：

| 变量名 | 值 | 说明 |
| :--- | :--- | :--- |
| `FBSPIDER_API_KEY` | 由管理员提供 | API Key，格式 `fbk_xxxxx` |
| `FBSPIDER_BASE_URL` | `http://localhost:7150` | 后端服务地址（本地部署固定为此值） |

**设置方法（Windows 系统环境变量）**：

1. 按 `Win+R`，输入 `sysdm.cpl`，回车
2. 点击 **"高级"** 标签 → **"环境变量"** 按钮
3. 在"用户变量"区域，点击 **"新建"**
4. 添加两个变量：
   - 变量名：`FBSPIDER_API_KEY`，变量值：你的 API Key
   - 变量名：`FBSPIDER_BASE_URL`，变量值：`http://localhost:7150`
5. 点击 **确定** 保存

### 4.3 在 OpenClaw 中注册 Skill

在 OpenClaw 中配置 Skill 信息，使其能识别像素分享相关的指令。

具体注册方式取决于 OpenClaw 的版本，一般需要：

1. 打开 OpenClaw
2. 进入 **Skill 管理** 或 **插件设置** 页面
3. 添加新 Skill，指向 `fbspider_pixel_authorize.py`
4. 保存并重启 OpenClaw

> ⚠️ 这一步如果不确定如何操作，请联系开发人员协助配置。

---

## 🧪 第五步：测试功能

### 5.1 前置条件检查清单

开始测试前，请逐一确认以下项目：

- [ ] Docker Desktop 已启动，状态为 Engine running（绿色）
- [ ] 后端服务运行中（访问 http://localhost:7150 有页面）
- [ ] Chrome 插件已加载，Console 显示 WS 已连接
- [ ] Chrome 已登录 Facebook（访问 business.facebook.com 能正常显示）
- [ ] 已登录 fbspider.com
- [ ] 环境变量已设置（FBSPIDER_API_KEY 和 FBSPIDER_BASE_URL）
- [ ] OpenClaw 已启动

### 5.2 测试一：像素分享给广告账户（自然语言）

在 OpenClaw 中输入：

```
把像素 NYHH058 分享给广告账户 1592610647817751，BM 是 2001982753911515
```

**预期结果**：OpenClaw 自动调用 Skill，返回分享成功信息。

### 5.3 测试二：像素 BM 间分享（自然语言）

在 OpenClaw 中输入：

```
把像素 2378488666008834 分享给 BM 1295757529414325
```

**预期结果**：OpenClaw 自动调用 Skill，返回授权成功信息。

### 5.4 测试三：命令行直接测试（排查用）

如果 OpenClaw 调用失败，可以用命令行直接测试后端是否正常。

**检查设备在线**：

```
curl -s http://localhost:7150/api/open/pixel/devices -H "X-API-Key: 你的API_Key"
```

**像素分享给广告账户**：

```
curl -s -X POST http://localhost:7150/api/open/pixel/share_to_ad_account -H "Content-Type: application/json" -H "X-API-Key: 你的API_Key" -d "{\"pixel_id\":\"1593764715035111\",\"pixel_name\":\"NYHH058\",\"target_account_ids\":[\"1592610647817751\"],\"business_id\":\"2001982753911515\"}"
```

**查询任务结果**（替换 task_id）：

```
curl -s http://localhost:7150/api/open/pixel/result/任务ID -H "X-API-Key: 你的API_Key"
```

### 5.5 确认分享生效

前往 Facebook BM 设置页：

```
https://business.facebook.com/latest/settings/events_dataset_and_pixel/?business_id=你的BM_ID
```

检查目标广告账户是否已关联该像素。

---

## 📖 Skill 可用方法一览

以下方法均可通过 OpenClaw 自然语言调用：

### 像素 BM 间分享

| 方法 | 说明 | 示例自然语言 |
| :--- | :--- | :--- |
| `authorize()` | 单次像素分享给 BM | "把像素 xxx 分享给 BM yyy" |
| `batch_authorize()` | 批量分享 | "把这几个像素分享给 BM yyy" |
| `authorize_from_file()` | 从文件读取批量分享 | "按文件 pixel_tasks.txt 执行分享" |

### 像素分享给广告账户

| 方法 | 说明 | 示例自然语言 |
| :--- | :--- | :--- |
| `share_to_ad_account()` | 单次像素分享给广告账户 | "把像素 NYHH058 分享给广告账户 xxx" |
| `batch_share_to_ad_account()` | 批量分享给广告账户 | "把这些像素分享给广告账户 xxx" |
| `share_to_ad_account_from_file()` | 从文件读取批量分享 | "按文件 ad_tasks.txt 分享像素给广告账户" |

### 设备管理

| 方法 | 说明 | 示例自然语言 |
| :--- | :--- | :--- |
| `get_online_devices()` | 查看在线插件设备 | "查看在线设备" |

---

## ❓ 常见问题排查

### Docker 相关

| 问题 | 解决方案 |
| :--- | :--- |
| `docker compose up` 报错 "failed to connect to docker API" | Docker Desktop 没启动，打开 Docker Desktop 等待 Engine running 后重试 |
| 端口 7150 被占用 | 执行 `netstat -ano | findstr 7150` 查看占用进程，关闭或修改端口 |
| 构建很慢 | 首次构建需下载基础镜像，取决于网速，耐心等待 |

### 插件相关

| 问题 | 解决方案 |
| :--- | :--- |
| 插件加载报错 | 确认选择的是 `fbspider v2.2.4` 文件夹（里面要有 `manifest.json`） |
| Console 中没有 PixelShare 日志 | 点击插件列表中 fbspider 下的 **"Service Worker"** 链接，在新窗口查看 |
| WS 连不上 | 确认后端服务正在运行（访问 http://localhost:7150 有页面） |
| 设备列表为空 | 插件未连接 WS，检查 Service Worker 日志中的错误信息 |

### OpenClaw / Skill 相关

| 问题 | 解决方案 |
| :--- | :--- |
| OpenClaw 无法调用 Skill | 检查 `fbspider_pixel_authorize.py` 是否在正确目录 |
| "Unauthorized" 错误 | 检查 `FBSPIDER_API_KEY` 环境变量是否设置正确 |
| "没有在线设备" | 检查插件是否已加载且 WS 已连接 |
| "无法获取 fb_dtsg token" | Chrome 中未登录 Facebook，先登录后再试 |
| 搜索不到像素 | 确认像素名称拼写正确，或尝试直接提供 pixel_asset_id |
| Skill 调用超时 | 检查后端服务是否运行（`docker compose ps`） |

---

## 📝 需要提供的测试数据

测试前由管理员或开发人员提供以下信息：

| 数据项 | 说明 | 示例 |
| :--- | :--- | :--- |
| API Key | 后端鉴权密钥 | `fbk_xxxxxxxxxxxxxxxxxx` |
| 像素名称 | fbspider 上显示的像素名称 | `NYHH058` |
| 像素 ID | fbspider 上显示的像素编号 | `1593764715035111` |
| BM ID | 像素所属的 Business Manager ID | `2001982753911515` |
| 广告账户 ID | 要分享给的目标广告账户 ID | `1592610647817751` |

---

## 📞 联系人

遇到问题请联系开发人员，提供以下信息以便快速排查：

1. 具体报错信息（截图或文字）
2. OpenClaw 中的完整对话记录
3. Service Worker Console 日志（F12 → 点插件的 Service Worker 链接 → Console）
4. 后端日志（命令提示符执行 `docker compose logs --tail=50`）
