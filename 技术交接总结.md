# 技术交接总结

## 一、项目整体介绍

### 1.1 项目名称

fbspider 自动化像素授权系统

### 1.2 项目目标

将人工在 fbspider.com 上点击操作像素授权的流程，改造为通过 OpenClaw 远程调用的自动化系统，减少人工操作成本。

### 1.3 系统组成

| 组件 | 目录 | 说明 |
|------|------|------|
| 浏览器插件 | `fbspider v2.2.4/` | Chrome Extension，在 fbspider.com 页面执行自动化操作 |
| 后端服务 | `fbspider-server/` | Flask + WebSocket 中继，桥接插件与外部调用方 |
| Skill 封装 | `fbspider-server/fbspider_pixel_authorize.py` | Python 类，封装后端 API 供 OpenClaw 调用 |

### 1.4 整体数据流

```
OpenClaw / 外部系统
       │
       │ 调用 Skill.authorize()
       ▼
  fbspider-server (Flask HTTP API :7150)
       │
       │ POST /api/open/pixel/authorize
       │ 查找在线设备 → 生成 task_id
       ▼
  fbspider-server (WebSocket 中继 :7671)
       │
       │ 下发 authorize_pixel 指令
       ▼
  fbspider 插件 (content-pixel.js)
       │
       │ 在 fbspider.com 页面执行授权操作
       │ （调 API 或模拟点击）
       │
       │ 上报 task_result
       ▼
  fbspider-server (WebSocket 中继)
       │
       │ 缓存结果到 task_results
       ▼
  Skill 轮询 /api/open/pixel/result/{task_id}
       │
       │ 返回最终结果
       ▼
  OpenClaw
```

---

## 二、当前开发状态

### 2.1 已完成

| 模块 | 状态 | 说明 |
|------|------|------|
| Flask 主应用 | ✅ 已完成 | 蓝图注册、CORS、gzip、SPA 托管 |
| MongoDB 数据模型 | ✅ 已完成 | 用户、API Key、广告账户、像素等全部集合和索引 |
| 认证体系 | ✅ 已完成 | Session / Token / API Key 三种方式 |
| WebSocket 中继 | ✅ 已完成 | 设备注册、心跳、指令下发、结果接收 |
| HTTP API — 像素授权 | ✅ 已完成 | authorize / result / devices 三个端点 |
| HTTP API — 开放接口 | ✅ 已完成 | 广告开关、预算修改、导航、点击等 |
| API Key 管理 | ✅ 已完成 | 增删查撤，scope 权限控制 |
| DSL 定时回调 | ✅ 已完成 | 批量推送广告账户数据到外部系统 |
| React 管理面板 | ✅ 已完成 | Dashboard、设备控制、像素管理等页面 |
| Skill 封装 | ✅ 已完成 | FbspiderPixelAuthorize 类 |
| 插件 WebSocket 通信 | ✅ 已完成 | 连接、注册、心跳、指令监听、结果上报 |
| Docker 部署 | ✅ 已完成 | Dockerfile + docker-compose.yml |

### 2.2 待完成 / 需确认

| 模块 | 状态 | 说明 |
|------|------|------|
| 插件 `authorizeViaAPI` | ⚠️ TODO | API 地址和请求体需要根据实际抓包结果填入 |
| 插件 `authorizeViaClick` | ⚠️ TODO | CSS 选择器需要根据实际页面 DOM 结构确认 |
| 插件 `getUsername` | ⚠️ 待验证 | 需要确认 fbspider.com 的用户名提取逻辑是否正确 |
| manifest.json content_scripts 配置 | ⚠️ 待确认 | content-pixel.js 是否已加入 content_scripts 匹配规则 |
| 端到端联调 | ❌ 未开始 | 需要真实 FB 资产进行全链路测试 |

---

## 三、关键配置信息

### 3.1 服务器

| 项目 | 地址 |
|------|------|
| 后端 API | `http://47.129.247.139:7150` |
| WebSocket | `ws://47.129.247.139:7671` |
| MongoDB | `54.179.56.204:27017`（数据库：plat） |
| MinIO | `54.179.56.204:9000`（桶：ad-creatives） |

### 3.2 默认账户

| 系统 | 用户名 | 密码 |
|------|--------|------|
| 管理面板 | admin | admin123456 |
| fbspider.com | liaoyu354@gmail.com | liaoyu59@ |

### 3.3 API Key

需要具有 `device-control` scope 的 API Key 才能调用像素授权接口。通过管理面板或 API 创建：

```bash
curl -X POST http://47.129.247.139:7150/api/keys \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "name": "pixel-authorize", "scopes": ["device-control"]}'
```

---

## 四、核心代码文件索引

### 4.1 后端服务

| 文件 | 核心职责 | 关键函数/类 |
|------|----------|-------------|
| `app.py` | 应用入口、蓝图注册 | `app` Flask 实例 |
| `config.py` | 配置管理 | 环境变量读取 |
| `auth.py` | 认证体系 | `get_current_user()`, `api_key_required()`, `api_admin_required()` |
| `models.py` | 数据库操作 | `init_db()`, `create_api_key()`, `upsert_ad_account()`, `get_user_by_api_key()` |
| `ws_relay.py` | WebSocket 中继 | `start_ws_server()`, `send_command()`, `pick_device()`, `find_device_by_username()` |
| `scheduler.py` | 定时 DSL 回调 | `start_scheduler()` |
| `routes/api_pixel.py` | 像素授权 API | `authorize()`, `result()`, `devices()` |
| `routes/api_open.py` | 开放 API | `send()`, `navigate()`, `click()`, `toggle()`, `budget()` |
| `routes/api_keys.py` | API Key 管理 | `create_key()`, `list_keys()`, `revoke_key()` |
| `fbspider_pixel_authorize.py` | Skill 封装 | `FbspiderPixelAuthorize.authorize()` |

### 4.2 浏览器插件

| 文件 | 核心职责 | 关键函数 |
|------|----------|----------|
| `manifest.json` | 插件配置、权限声明 | content_scripts 匹配规则 |
| `content/content-pixel.js` | 像素授权核心逻辑 | `handleAuthorizePixel()`, `authorizeViaAPI()`, `authorizeViaClick()` |
| `content/content-main.js` | fbspider.com 页面主逻辑 | （已混淆） |
| `background.js` | 后台服务 | （已混淆） |
| `popup.js` | 插件弹窗 | （已混淆） |

---

## 五、部署与运维

### 5.1 部署命令

```bash
# Docker 部署
cd fbspider-server
docker compose up -d --build

# 查看日志
docker logs -f fbhelper-server
```

### 5.2 健康检查

| 检查项 | 方法 |
|--------|------|
| HTTP 服务 | `curl http://47.129.247.139:7150/dashboard` |
| WebSocket | 查看日志中 `[WS] WebSocket server listening on ws://0.0.0.0:7671` |
| 设备在线 | `curl -H "X-API-Key: fbk_xxxxx" http://47.129.247.139:7150/api/open/pixel/devices` |
| MongoDB | 检查 `plat` 数据库集合是否正常 |

### 5.3 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| `ERR_CONTENT_LENGTH_MISMATCH` | VPN 代理 gzip 冲突 | 已在 after_request 中处理，确认 gunicorn 未开启 gzip |
| WebSocket 连不上 | 端口未开放 / 防火墙 | 检查 7671 端口是否开放 |
| API Key 鉴权失败 | Key 无 device-control scope | 重新创建带 scope 的 Key |
| 无在线设备 | 插件未连接 / 未打开 fbspider.com | 确认插件已安装并在 fbspider.com 页面 |
| 任务超时 | 插件未响应 | 检查插件 console 日志 |

---

## 六、后续工作建议

### 6.1 优先级高（建议立即处理）

1. **抓包确认 fbspider.com 像素授权 API**：打开浏览器 F12，手动操作一次像素授权，记录 Network 请求，更新 `authorizeViaAPI()` 中的接口地址和参数
2. **确认 manifest.json 中 content-pixel.js 的注入规则**：目前 `manifest.json` 的 content_scripts 中没有看到 `content-pixel.js` 的匹配项，需要添加
3. **端到端联调测试**

### 6.2 优先级中

1. **批量像素授权**：支持一次传入多个 pixel_id + account_id 对
2. **任务持久化**：目前 task_results 在内存中，服务重启会丢失，考虑存 MongoDB
3. **结果缓存过期清理**：避免 task_results 无限增长

### 6.3 优先级低

1. **监控面板**：在线设备数、任务成功率、平均耗时
2. **像素权限撤销**
3. **像素分享记录查询**

---

## 七、相关文档索引

| 文档 | 位置 |
|------|------|
| 后端代码实现文档 | `fbspider-server/后端代码实现文档.md` |
| 浏览器插件开发文档 | `fbspider v2.2.4/浏览器插件开发文档.md` |
| 像素授权技术方案 | `fbspider像素授权技术方案.md` |
| Skill 开发文档 | `fbspider-server/Skill开发文档.md` |
| 广告控制 API 文档 | `fbspider-server/FbHelper_API.md` |
| 需求概述 | `fbspider像素授权需求概述.txt` |

---

## 八、联系方式与资源

| 角色 | 人员 |
|------|------|
| 项目负责人 | 架构师 |
| 协助人员 | 01、ly |
| 文档版本 | v1.0 |
| 最后更新 | 2026-04-22 |
