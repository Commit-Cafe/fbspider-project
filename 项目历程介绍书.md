# Fbspider 像素授权自动化系统 — 项目历程介绍书

> **文档版本**: v3.0（整合版）
> **整合日期**: 2026-05-07
> **涵盖时间**: 2026-04 ~ 2026-05

---

## 目录

1. [项目背景与目标](#一项目背景与目标)
2. [系统架构](#二系统架构)
3. [技术实现](#三技术实现)
4. [项目历程](#四项目历程)
5. [已完成的改造](#五已完成的改造)
6. [测试验证](#六测试验证)
7. [关键配置信息](#七关键配置信息)
8. [代码文件索引](#八代码文件索引)
9. [部署与运维](#九部署与运维)
10. [后续规划](#十后续规划)
11. [已知问题与风险](#十一已知问题与风险)

---

## 一、项目背景与目标

### 1.1 需求起源

Facebook 广告投放中，**像素（Pixel）的 BM 间分享**是高频重复操作。投手需要手动在 fbspider.com 网站上逐个点击授权，将像素从一个 Business Manager 分享到另一个 Business Manager。

**痛点**：
- 50 个像素 × 10 个账户 = 500 次手动操作，每次约 30 秒，总计 4 小时以上
- 操作枯燥，容易出错
- 无法远程操控，必须守在电脑前

### 1.2 项目目标

将人工在 fbspider.com 上点击操作像素授权的流程，改造为通过 AI Agent（OpenClaw / nanobot）输入自然语言即可完成的自动化系统：

```
用户输入：把像素 969843995418298 从 BM 1704095947618184 分享给 BM 1295757529414325
系统输出：授权结果（成功/失败 + 原因）
```

### 1.3 设计原则

| 原则 | 说明 |
|------|------|
| 不修改官方代码 | fbspider 官方插件代码（background.js 等）经过混淆，不触碰 |
| 直接调 API | 不模拟鼠标键盘，直接调用 Facebook GraphQL API，速度快、稳定性高 |
| 真实数据返回 | 成功就是成功，失败就是失败，不做任何预处理或缓存欺骗用户 |
| 架构可复用 | 三层架构设计，后续扩展其他功能（主页分享、BM分享等）只需新增 action |

---

## 二、系统架构

### 2.1 整体架构图

```
用户（自然语言指令）
    │
    ▼
┌──────────────────────────┐
│  AI Agent 层             │
│  OpenClaw (GLM-5.1)      │
│  nanobot (DeepSeek-v4)   │
│                          │
│  解析意图 → 生成 CLI 命令 │
└────────┬─────────────────┘
         │ exec: python pixel_authorize.py authorize ...
         ▼
┌──────────────────────────┐
│  Skill 执行层            │
│  pixel_authorize.py      │
│                          │
│  CLI 脚本，构造 HTTP 请求 │
│  轮询等待结果             │
└────────┬─────────────────┘
         │ HTTP POST (API Key 认证)
         ▼
┌──────────────────────────────────────────────┐
│  后端中继层 (fbspider-server)                  │
│                                              │
│  ┌──────────────┐  ┌────────────────────┐    │
│  │ Flask HTTP   │  │ WebSocket 中继     │    │
│  │ API :7150    │←→│ :7671             │    │
│  │              │  │                    │    │
│  │ API Key 鉴权 │  │ 设备注册/心跳      │    │
│  │ 任务管理     │  │ 指令下发           │    │
│  │ 结果查询     │  │ 结果接收           │    │
│  └──────┬───────┘  └───────┬────────────┘    │
│         │                  │                  │
│  ┌──────▼──────────────────▼──────────────┐  │
│  │  MongoDB (plat 数据库)                  │  │
│  │  用户 / API Key / 广告账户 / 像素 / 日志 │  │
│  └────────────────────────────────────────┘  │
└──────────────────────┬───────────────────────┘
                       │ WebSocket 长连接
                       ▼
┌──────────────────────────────────────────────┐
│  插件执行层 (fbspider v2.2.4)                  │
│                                              │
│  sw-wrapper.js (Service Worker 入口)          │
│  ├── background.js   ← 官方代码（混淆）       │
│  └── pixel-share.js  ← 自开发（24KB）         │
│       │                                      │
│       │ 1. 收到授权指令                        │
│       │ 2. 提取 Facebook Token (fb_dtsg等)    │
│       │ 3. 调用 Facebook GraphQL API 分享     │
│       │ 4. 解析结果 + 错误翻译为中文           │
│       │ 5. 通过 WS 回传结果                   │
│       │                                      │
│  content-pixel.js  ← 页面 username 提取       │
└──────────────────────┬───────────────────────┘
                       │ Facebook GraphQL
                       ▼
              Facebook 服务器
        (business.facebook.com/api/graphql/)
```

### 2.2 三层分工

| 层 | 组件 | 职责 | 关键技术 |
|-----|------|------|---------|
| AI Agent 层 | OpenClaw / nanobot | 理解自然语言，生成 CLI 命令 | GLM-5.1 / DeepSeek，TOOLS.md skill 注入 |
| 后端中继层 | fbspider-server | API Key 鉴权、设备管理、任务分发、结果缓存 | Flask + WebSocket (gevent)，MongoDB |
| 插件执行层 | fbspider v2.2.4 | 实际操作 Facebook，完成像素分享 | Chrome Extension MV3，Facebook GraphQL API |

### 2.3 技术栈

| 模块 | 技术 | 说明 |
|------|------|------|
| HTTP 框架 | Flask 3.x | RESTful API |
| WebSocket | websockets 13.x+ | 插件长连接通信 |
| 数据库 | MongoDB 4.x+ (pymongo) | 业务数据持久化 |
| 前端面板 | React + Vite | 管理面板 SPA |
| 进程管理 | gunicorn + gevent | 生产部署（workers=1） |
| 对象存储 | MinIO 7.x+ | 广告素材存储 |
| 浏览器插件 | Chrome Extension MV3 | Service Worker 架构 |
| AI Agent | OpenClaw / nanobot | 自然语言 → 命令执行 |

---

## 三、技术实现

### 3.1 数据流（单次授权完整路径）

```
步骤 1: 用户输入
  "把像素 123 从 BM A 分享给 BM B"
       │
       ▼
步骤 2: AI Agent 解析意图 → 执行 CLI 命令
  python pixel_authorize.py authorize --pixel-id 123 --target-bm B --business-id A
       │
       ▼
步骤 3: 脚本发 HTTP POST → Flask 后端
  POST /api/open/pixel/authorize
  Headers: X-API-Key: fbk_xxx
  Body: { pixel_id: "123", target_account_id: "B", business_id: "A" }
       │
       ▼
步骤 4: Flask 后端验证 API Key → 查找在线设备 → 通过 WS 下发指令
  { action: "authorize_pixel", task_id: "abc123", params: { pixel_id: "123", ... } }
       │
       ▼
步骤 5: Chrome 插件收到指令 → 执行分享
  a. fetch business.facebook.com → 提取 fb_dtsg, lsd, userId, bmId
  b. 先尝试 fbspider 原版接口（background.js → callSharePixelScript）
  c. 失败则回退到 GraphQL 直接调用：
     - POST GraphQL 验证请求 (DocID: 27487406087539832)
     - POST GraphQL 分享请求 (DocID: 9104467643012543)
  d. 解析响应：
     - result_type=SUCCESS → 分享成功
     - result_type=FAILURE + 目标BM在合作伙伴列表 → 已拥有权限
     - result_type=FAILURE + missing_required_variable_value → BM受限
       │
       ▼
步骤 6: 插件通过 WS 回传结果 → Flask 后端缓存
  { type: "task_result", task_id: "abc123", result: {...} }
       │
       ▼
步骤 7: 脚本轮询 GET /api/open/pixel/result/abc123 → 获取结果
       │
       ▼
步骤 8: AI Agent 格式化展示给用户
```

### 3.2 双路径回退机制

插件分享像素时有两条路径：

```
路径 1（优先）: shareViaFbspider
  调用 background.js 的 callSharePixelScript → fbspider 远程服务器
  ↓ 失败（"Could not establish connection"）
路径 2（回退）: shareViaGraphql
  直接调用 Facebook GraphQL API (business.facebook.com/api/graphql/)
  ↓
  提取 Token → 验证查询 → 分享请求 → 解析结果
```

### 3.3 设备路由策略

后端根据参数优先级查找在线设备：

| 优先级 | 参数 | 路由方式 | 适用场景 |
|--------|------|----------|---------|
| 1 | `device` | 按设备 ID 前缀匹配 | 调试时指定特定设备 |
| 2 | `account_id` | 广告账户 → 用户 → 设备 | 广告操作自动路由 |
| 3 | `username` | 用户名 → 匹配设备 | 像素授权场景（推荐） |
| 4 | 无参数 | 第一个在线设备 | 只有一个设备时 |

### 3.4 认证体系

后端支持三种认证方式：

| 方式 | 请求头 | 适用场景 |
|------|--------|---------|
| API Key | `X-API-Key: fbk_xxx` | 外部系统/Agent 调用 |
| Bearer Token | `Authorization: Bearer xxx` | 管理面板登录 |
| Session Cookie | `session` Cookie | 页面访问 |

API Key 安全设计：
- 格式：`fbk_` + 40 字符随机 token
- 数据库仅存 SHA256 哈希，明文只创建时显示一次
- 支持 scope 权限控制（像素授权需 `device-control` scope）
- 支持过期时间和即时撤销

### 3.5 错误翻译机制

Facebook 返回的技术错误自动翻译为用户友好的中文：

| Facebook 返回 | 用户看到 |
|--------------|---------|
| `result_type=SUCCESS` | "分享成功" |
| 目标BM已在合作伙伴列表 | "目标 BM 已拥有该像素权限（无需重复分享）" |
| `missing_required_variable_value` | "当前BM受限，该BM的像素无法分享" |
| 源BM = 目标BM | "无法分享给自己" |
| `noncoercible_argument_value` | "当前BM受限，该BM的像素无法分享" |

翻译逻辑合并验证响应和分享响应的错误文本，确保任一阶段出现的关键字都能被正确识别。

---

## 四、项目历程

### 4.1 时间线

```
2026-04-22  项目启动
            ├── 需求分析：减少人工点击 fbspider 授权像素的操作
            ├── 架构设计：Chrome 插件 + Flask 后端 + WebSocket 中继
            ├── 基础设施搭建：Flask 骨架、WebSocket 中继、MongoDB 模型
            └── 文档编写：技术方案、交接总结、学习笔记

2026-04-29  插件开发
            ├── content-pixel.js 初始版本（WS 连接 + 注册 + 心跳）
            ├── 后端 API 路由（authorize / result / devices）
            ├── Skill 封装（FbspiderPixelAuthorize 类）
            └── 文档更新：Skill 开发文档、插件开发文档

2026-05-01 ~ 05-05  核心突破
            ├── 发现 fbspider 原版接口不可用（background.js 混淆）
            ├── 转向直接调用 Facebook GraphQL API
            ├── pixel-share.js 开发（替代 content-pixel.js）
            │   ├── Token 自动提取（fb_dtsg, lsd, userId, bmId）
            │   ├── GraphQL 验证查询 (DocID: 27487406087539832)
            │   ├── GraphQL 分享请求 (DocID: 9104467643012543)
            │   └── 结果解析 + 合作伙伴列表检查
            ├── sw-wrapper.js 入口设计（并行加载官方 + 自开发模块）
            └── 错误翻译机制

2026-05-06  端到端联调
            ├── 本地后端启动测试（127.0.0.1:7150 + WS 7671）
            ├── Chrome 插件连接验证
            ├── nanobot 全链路测试
            ├── 发现并修复 4 个 Bug：
            │   ├── shareViaFbspider 失败时应 reject 而非 resolve
            │   ├── shareViaGraphql catch 中不应 reject 未捕获异常
            │   ├── 心跳 username 应传实际值而非 null
            │   └── WS_URL 应支持配置而非硬编码
            ├── 错误翻译逻辑增强（合并验证+分享响应）
            └── task_results 缓存清理机制

2026-05-07  功能验证 + 迁移
            ├── nanobot 全流程测试通过
            │   ├── API Key 认证 ✅
            │   ├── 设备路由 ✅
            │   ├── 单次授权 ✅
            │   ├── 批量授权 ✅
            │   └── 错误翻译 ✅
            ├── 补充 --business-id 参数支持
            ├── 迁移到 OpenClaw (GLM-5.1)
            │   ├── 创建 plugin-skills/fbspider-pixel-authorize/
            │   ├── 写入 TOOLS.md 确保 GLM-5.1 能识别工具
            │   └── 全流程测试通过
            ├── 代码推送到 Gitea 私有仓库
            │   ├── fbhelper (Chrome 插件) → 172.31.45.252:3001/liujiyu/fbhelper
            │   └── fbspider-server (后端) → 172.31.45.252:3001/liujiyu/fbspider-server
            └── 整合项目文档为历程介绍书
```

### 4.2 关键决策记录

| 时间点 | 决策 | 原因 |
|--------|------|------|
| 项目初期 | 不修改官方混淆代码 | background.js 等经 obfuscator 加密，不可逆 |
| 05-01 | 从 fbspider API 转向 Facebook GraphQL | fbspider 接口需要 background.js 支持但无法正常通信 |
| 05-01 | 用 sw-wrapper.js 并行加载 | 保留官方功能的同时注入自开发模块 |
| 05-06 | 心跳传递 username | 确保后端能按用户名路由设备 |
| 05-06 | 合并验证+分享错误文本 | missing_required_variable_value 只在验证阶段出现 |
| 05-07 | TOOLS.md 写入工具说明 | GLM-5.1 不像 DeepSeek 自动扫描 skill 目录 |

---

## 五、已完成的改造

| # | 改造项 | 状态 | 说明 |
|---|--------|------|------|
| 1 | WS 中继连接（插件 ↔ 后端） | ✅ | WebSocket :7671，自动重连 |
| 2 | Facebook GraphQL 直接调用 | ✅ | 绕过 fbspider 官方接口 |
| 3 | Token 自动提取 | ✅ | fb_dtsg, lsd, userId, bmId |
| 4 | business_id 优先级修复 | ✅ | 指定值 > 自动检测 |
| 5 | result_type: FAILURE 错误检测 | ✅ | 递归解析 GraphQL 响应 |
| 6 | 已分享像素自动识别 | ✅ | 检查合作伙伴列表 |
| 7 | 文件格式四列支持 | ✅ | 像素ID, 所属BM, 所有者, 目标BM |
| 8 | 同BM跳过 | ✅ | _skip 标记 |
| 9 | 技术错误中文翻译 | ✅ | 合并验证+分享响应 |
| 10 | WS_URL 可配置 | ✅ | chrome.storage.local 覆盖 |
| 11 | nanobot skill 集成 | ✅ | ~/.nanobot/workspace/skills/ |
| 12 | OpenClaw skill 迁移 | ✅ | ~/.openclaw/plugin-skills/ + TOOLS.md |
| 13 | --business-id 参数 | ✅ | 确保使用正确的源 BM |
| 14 | task_results 缓存清理 | ✅ | 超过 100 条时清理 30 分钟前的记录 |
| 15 | Gitea 私有仓库 | ✅ | fbhelper + fbspider-server |

---

## 六、测试验证

### 6.1 端到端测试结果（2026-05-06）

测试环境：本地后端 127.0.0.1:7150，Chrome 插件已连接

| 像素 ID | 源 BM | 目标 BM | 结果 | 原因 |
|---------|-------|---------|------|------|
| 2378488666008834 | 1281455183398723 | 1295757529414325 | ✅ | 已拥有权限 |
| 965817239731745 | 1281455183398723 | 1295757529414325 | ✅ | 已拥有权限 |
| 1639781780563928 | 1704095947618184 | 1295757529414325 | ✅ | 已拥有权限 |
| 1108640238122953 | 1704095947618184 | 1295757529414325 | ✅ | 已拥有权限 |
| 1584147626007698 | 977630551490199 | 1295757529414325 | ❌ | BM受限 |
| 969843995418298 | 1704095947618184 | 1295757529414325 | ❌ | BM受限 |

### 6.2 nanobot 全流程测试（2026-05-07）

```
用户输入: 把像素 969843995418298 从 BM 1704095947618184 分享给 BM 1295757529414325
执行命令: pixel_authorize.py authorize --pixel-id 969843995418298 --target-bm 1295757529414325 --business-id 1704095947618184
结果: ❌ 当前BM受限，该BM的像素无法分享（WhatsApp Marketing Message Event Sharing）。源 BM: 1704095947618184
```

**验证点**：
- ✅ --business-id 生效，源 BM 正确识别
- ✅ 实时请求 Facebook，非缓存
- ✅ 错误翻译为中文
- ✅ 包含像素名称、源 BM、目标 BM
- ✅ 与人工操作结果一致

### 6.3 OpenClaw 全流程测试（2026-05-07）

- ✅ GLM-5.1 通过 TOOLS.md 识别工具
- ✅ 正确生成 CLI 命令
- ✅ 全链路结果正确返回

---

## 七、关键配置信息

### 7.1 服务器地址

| 项目 | 地址 |
|------|------|
| 后端 API | `http://47.129.247.139:7150`（远端）/ `http://127.0.0.1:7150`（本地） |
| WebSocket | `ws://47.129.247.139:7671` / `ws://127.0.0.1:7671` |
| MongoDB | `54.179.56.204:27017`（数据库：plat） |
| MinIO | `54.179.56.204:9000`（桶：ad-creatives） |
| Gitea | `http://172.31.45.252:3001/liujiyu/` |

### 7.2 GraphQL 接口

| 用途 | DocID |
|------|-------|
| 分享请求 | `9104467643012543` |
| 验证查询 | `27487406087539832` |
| GraphQL URL | `https://business.facebook.com/api/graphql/` |

### 7.3 端口分配

| 端口 | 协议 | 用途 |
|------|------|------|
| 7150 | HTTP | Flask API + React 管理面板 |
| 7671 | WebSocket | 插件实时通信 |

### 7.4 默认账户

| 系统 | 用户名 | 密码 |
|------|--------|------|
| 管理面板 | admin | admin123456 |
| fbspider.com | liaoyu354@gmail.com | liaoyu59@ |

### 7.5 API Key

测试用 API Key：`fbk_0XQ-X29WIksAh1Y1JXorzTpnGHye--pcSwjQDuvJH2VG8WYDSd-CPA`
- 需具备 `device-control` scope
- 存储在 MongoDB 的 `fbhelper_api_key` 集合中

### 7.6 AI Agent 路径

| Agent | 路径 |
|-------|------|
| nanobot | `C:\Users\42151\.nanobot\` |
| OpenClaw | `C:\Users\42151\.openclaw\` |

---

## 八、代码文件索引

### 8.1 浏览器插件 (fbspider v2.2.4)

```
fbspider v2.2.4/
├── manifest.json              # 插件配置（MV3）
├── sw-wrapper.js              # Service Worker 入口（自开发，168B）
│   加载 background.js + pixel-share.js
├── pixel-share.js             # 像素分享核心（自开发，24KB）
│   ├── WS 连接 + 注册 + 心跳
│   ├── Facebook Token 提取
│   ├── GraphQL 验证查询 + 分享请求
│   ├── 双路径回退（fbspider原版 → GraphQL）
│   ├── 结果解析 + 合作伙伴检查
│   └── 错误翻译为中文
├── background.js              # 官方后台脚本（混淆，221KB）
├── content/content-pixel.js   # 页面 username 提取（自开发，1.6KB）
├── content/content-main.js    # fbspider.com 主逻辑（混淆，20KB）
├── content/content-script.js  # Ads Manager 脚本（混淆，119KB）
├── popup.js                   # 弹窗逻辑（混淆，5.7KB）
└── ...
```

### 8.2 后端服务 (fbspider-server)

```
fbspider-server/
├── app.py                          # Flask 主应用入口
├── config.py                       # 配置（环境变量）
├── auth.py                         # 认证体系（Session/Token/API Key）
├── models.py                       # MongoDB 数据模型层
├── ws_relay.py                     # WebSocket 中继服务器
├── scheduler.py                    # 定时 DSL 回调
├── fbspider_pixel_authorize.py     # Skill 封装（Python 类）
├── routes/
│   ├── api_pixel.py               # 像素授权 API
│   ├── api_open.py                # 开放 API（广告开关/预算）
│   ├── api_keys.py                # API Key 管理
│   ├── api_device_control.py      # 设备控制（管理员）
│   ├── auth.py                    # 登录/登出
│   ├── api_receive.py             # 插件数据上报
│   ├── api_serve.py               # 插件数据查询
│   ├── api_commands.py            # 指令队列
│   ├── api_ads.py                 # 广告数据
│   └── api_upload.py              # 文件上传
├── frontend/                      # React 管理面板
└── requirements.txt               # Python 依赖
```

### 8.3 AI Agent Skill 文件

```
nanobot:
  ~/.nanobot/workspace/skills/fbspider-pixel-authorize/
  ├── SKILL.md                     # Skill 定义
  └── scripts/pixel_authorize.py   # CLI 执行脚本

OpenClaw:
  ~/.openclaw/plugin-skills/fbspider-pixel-authorize/
  ├── SKILL.md                     # Skill 定义
  └── scripts/pixel_authorize.py   # CLI 执行脚本
  ~/.openclaw/workspace/TOOLS.md   # 工具使用说明（GLM-5.1 必读）
```

---

## 九、部署与运维

### 9.1 Docker 部署（推荐）

```bash
cd fbspider-server
docker compose up -d --build
```

### 9.2 手动部署

```bash
# 1. 安装依赖
pip install -r requirements.txt gunicorn

# 2. 编译前端
cd frontend && npm ci && npm run build && cd ..

# 3. 启动
gunicorn --bind 0.0.0.0:7150 --worker-class gthread --threads 8 --workers 1 app:app
```

### 9.3 小白用户部署清单

如果后端部署在服务器上，小白电脑只需：

1. **安装 Python + OpenClaw**（可做成一键脚本）
2. **安装 Chrome 插件** + 登录 Facebook (business.facebook.com)
3. **复制 skill 文件** 到 `~/.openclaw/plugin-skills/`
4. **启动 OpenClaw**，输入自然语言指令

### 9.4 健康检查

| 检查项 | 方法 |
|--------|------|
| HTTP 服务 | `curl http://127.0.0.1:7150/dashboard` |
| WebSocket | 查看日志 `[WS] WebSocket server listening on ws://0.0.0.0:7671` |
| 设备在线 | `curl -H "X-API-Key: fbk_xxx" http://127.0.0.1:7150/api/open/pixel/devices` |
| 插件状态 | Chrome Console 搜索 `[PixelShare]` |

### 9.5 常见问题排查

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| `Unauthorized` | API Key 无效或缺失 | 检查 X-API-Key 头 |
| `API Key 缺少 scope` | Key 没有 device-control 权限 | 重建带 scope 的 Key |
| `没有在线设备` | 插件未连接 | 确认插件已装且在 fbspider.com 页面 |
| 任务一直 pending | 插件未响应 | 检查插件 console 日志 |
| `ERR_CONTENT_LENGTH_MISMATCH` | VPN 代理 gzip 冲突 | 已在 after_request 中处理 |
| WS 连不上 | 7671 端口未开放 | 检查防火墙 |

---

## 十、后续规划

### 10.1 功能扩展（按优先级）

| 优先级 | 功能 | 难度 | 复用程度 | 说明 |
|--------|------|------|---------|------|
| 1 | 像素分享给广告账户 | ⭐⭐ | 90% | 接口不同但流程一致 |
| 2 | 主页分享到个号 | ⭐⭐⭐ | 70% | 需处理角色类型 |
| 3 | 广告账户分享到邮箱 | ⭐⭐⭐⭐ | 50% | 涉及邀请流程 |
| 4 | BM 分享到个号 | ⭐⭐⭐⭐ | 50% | 涉及邀请 + 风控 |

### 10.2 系统优化

| 项目 | 说明 |
|------|------|
| 任务结果持久化 | 从内存迁移到 MongoDB，防重启丢失 |
| 安全加固 | MongoDB/MinIO 密码从硬编码改为环境变量 |
| CORS 收紧 | origins 从 `".*"` 改为白名单 |
| SECRET_KEY 加强 | 从硬编码改为随机生成 |
| 监控面板 | 在线设备数、任务成功率、平均耗时 |
| 一键部署包 | Python + OpenClaw + 插件 + 配置打包 |

---

## 十一、已知问题与风险

### 11.1 安全问题（待修复）

| 问题 | 文件 | 严重度 |
|------|------|--------|
| MongoDB/MinIO 密码明文硬编码 | config.py, docker-compose.yml | 高 |
| CORS `origins=r".*"` 过于宽松 | app.py | 中 |
| `SECRET_KEY = 'fbhelper-local-dev-key'` 过弱 | config.py | 中 |

### 11.2 运行时风险

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| Facebook GraphQL DocID 变更 | 授权失败 | 维护映射表，定期验证 |
| `fb_dtsg` token 过期 | 批量操作中断 | 每次操作前重新获取 |
| Facebook 频率限制 | 封号/限流 | 操作间隔 2-3 秒 |
| Facebook 安全验证弹窗 | 操作中断 | 需用户手动处理 |
| gunicorn 多 worker | WebSocket 状态不一致 | workers=1（已配置） |

---

## 附录：原始文档索引

本文档整合自以下 8 份文档：

| 原始文档 | 主要内容 |
|---------|---------|
| `Fbspider像素授权系统学习笔记.md` | 图文教程、概念讲解、架构图、应用场景 |
| `fbspider 项目交接小结.md` | 改造清单、API 说明、错误翻译表、测试结果 |
| `fbspider像素授权技术方案.md` | 通信协议设计、设备路由策略、授权流程、风险评估 |
| `技术交接总结.md` | 项目组成、开发状态、配置信息、代码索引、运维指南 |
| `浏览器插件开发文档.md` | 插件结构、manifest 配置、content-pixel.js 详解 |
| `后端代码实现文档.md` | Flask 架构、数据模型、路由模块、部署指南 |
| `Skill开发文档.md` | Skill 类定义、API 方法、OpenClaw 集成指南 |
| `FbHelper_API.md` | 广告控制接口（开关/预算）文档 |
